{
  "$schema": "https://raw.githubusercontent.com/googleapis/release-please/main/schemas/config.json",
  "include-v-in-tag": true,
  "release-type": "simple",
  "initial-version": "0.1.0",
  "bump-minor-pre-major": true,
  "bump-patch-for-minor-pre-major": true,
  "pull-request-title-pattern": "chore(release): prepare v${version}",
  "pull-request-header": "## IBEX version release\n\nThis pull request was opened by the **Version Release** workflow on `main`. Review `CHANGELOG.md` and the proposed semver before merge.",
  "pull-request-footer": "---\n\nMerging creates tag `v${version}` and triggers the Tagged Release workflow (GitHub Release, SBOM, cosign signature).",
  "label": "version-release: pending",
  "release-label": "version-release: tagged",
  "packages": {
    ".": {
      "changelog-path": "CHANGELOG.md"
    }
  }
}
