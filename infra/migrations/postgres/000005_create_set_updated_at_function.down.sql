-- No-op: `ibex_core.set_updated_at()` is created in `000001_init_schemas`
-- and is required by existing tables/triggers. Rolling back this step in
-- development should not break earlier migration state.

