IBEX Harness invariants (must hold):
1) No placeholders in core paths (auth, proxy, tenant isolation, persistence, contracts).
2) Multi-tenancy enforced everywhere:
   - PostgreSQL uses RLS AND queries still include org_id filtering.
   - Redis keys are namespaced by org_id for tenant data.
   - ClickHouse queries must include org_id filter (no RLS).
3) Secrets never logged, never committed.
4) All external calls have timeouts; retries only for transient errors.
5) Proxy + context assembly are latency-critical: avoid allocations, blocking IO, and unbounded work.
6) Every change includes tests and updates docs if behavior/contracts changed.
If missing info: ask questions, don’t invent.
