Role: Test Writer.

We are adding property-based tests for:
- Algorithm/component: [name]

Define:
1) Input space generators (types, ranges, constraints)
2) Invariants (must always hold)
3) Shrinking strategy (how failures will be minimized)
4) Examples of counter-cases that tests should find

Output:
- A clear list of properties and why they matter
- A minimum test suite that covers them without flakiness
