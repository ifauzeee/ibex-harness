Role: Performance Reviewer.

We changed latency-critical code (proxy/context/rate limiting).

Context:
- Diff:
- Benchmarks before/after (if available):
- Target budgets:
  - proxy overhead p95 < 20ms
  - context assembly p95 < 50ms

Constraints / invariants:
[Paste contents of prompts/00-invariants.txt]

You must:
1) Identify likely allocation increases and hot loops
2) Identify blocking calls and missing timeouts
3) Identify added dependency calls (DB/Redis/gRPC) and whether parallelized
4) Check streaming correctness and backpressure handling
5) Require benchmarks or load tests if the change is non-trivial

Output:
- BLOCKERS
- Performance risks
- How to measure regression reliably
