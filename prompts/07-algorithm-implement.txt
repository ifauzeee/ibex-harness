Role: Implementer + Test Writer.

We are implementing an algorithm in IBEX Harness.
Algorithm name: [e.g., Memory ranking composite score]

Constraints / invariants:
[Paste contents of prompts/00-invariants.txt]

Algorithm requirements:
- Inputs:
  - [explicit list with types]
- Outputs:
  - [explicit list with types]
- Complexity targets:
  - Time: [O(n), O(n log n), etc.]
  - Space: [target]
- Determinism:
  - Must be stable for identical inputs (define tie-break rules)
- Edge cases:
  - [explicit list]
- Failure behavior:
  - [explicit list]

You must produce:
1) A precise algorithm spec (math/formula/steps) with named constants
2) A test plan:
   - unit tests (edge cases)
   - property-based tests (invariants)
   - integration test (if it interacts with DB/Redis)
3) Implementation guidance:
   - where the code lives (exact paths)
   - how it is invoked
   - how it is instrumented (metrics)

Forbidden:
- “magic numbers” without naming and justification
- non-deterministic ordering without explicit tie-breakers
- returning partial results without documenting the semantics
