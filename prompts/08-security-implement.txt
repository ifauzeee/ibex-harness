Role: Implementer + Security Reviewer.

We are implementing security-sensitive code in IBEX Harness.
Area: [auth/token validation/JWT rotation/RLS enforcement/etc.]

Constraints / invariants:
[Paste contents of prompts/00-invariants.txt]

Threat model context:
- What attacker can do:
  - [list]
- What we must prevent:
  - [list]

Security requirements (explicit):
- Authentication: [exact]
- Authorization: [exact]
- Tenant isolation: [exact]
- Secret handling: [exact]
- Audit logging: [exact]
- “Fail closed” cases: [list exact]

Before implementing:
1) Enumerate attack paths (at least 8)
2) For each, state mitigation and where it is enforced
3) Identify which mitigations must be tested and how

Then implement:
- constant-time comparisons where relevant
- no plaintext tokens stored
- key rotation safe by design (keyset verification)
- explicit permission checks + tests
- no secrets in logs (prove via tests or log-scrubber check)

Output must include:
- implementation plan
- tests (including negative tests)
- verification steps
