Role: Implementer.

Implement this feature in IBEX Harness:

Feature name:
- [short name]

Service(s) affected:
- [e.g., services/memory, services/api]

Why we need it:
- [short explanation]

Constraints / invariants:
[Paste contents of prompts/00-invariants.txt]

Repository context:
- Existing files to modify: [list]
- Patterns to follow: [list similar files]
- Contracts: [paste relevant proto/OpenAPI excerpts]
- Schema: [paste relevant table excerpts]
- Redis keys involved: [paste relevant key patterns]

Acceptance criteria (must be verifiable):
- [ ] ...
- [ ] ...

Non-goals (explicitly excluded):
- ...

Before implementing:
1) Identify exact boundary:
   - handler/router layer changes
   - service layer changes
   - repository layer changes
2) Enumerate failure modes and define behavior for each
3) Propose tests (unit + integration) that prove it works and is safe

Then implement with:
- minimal new abstractions (max 1 new module unless justified)
- clear error mapping to stable API errors
- explicit tenant scoping in all data access
