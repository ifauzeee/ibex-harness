Role: Test Writer.

We need integration tests using real dependencies.

Component under test:
- [service/module]

Dependencies:
- [Postgres pgvector / Redis / ClickHouse / MinIO]
Schema/migrations required:
- [paths]

Constraints / invariants:
[Paste contents of prompts/00-invariants.txt]

Provide:
1) Testcontainer setup approach (fast, isolated)
2) Data setup strategy (unique org_id per test)
3) Tests:
   - happy path
   - cross-tenant isolation attempt
   - failure path (e.g., Redis down / DB failover simulation if feasible)
4) Cleanup strategy (rollback transactions / truncate tables)
5) How to run tests locally + in CI reliably
