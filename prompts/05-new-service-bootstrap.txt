Role: Implementer.

We are bootstrapping a new service in IBEX Harness.

Service name: [e.g., services/auth]
Language/framework: [Go net/http | Python FastAPI | Next.js App Router]
Service purpose: [one paragraph]

Constraints / invariants:
[Paste contents of prompts/00-invariants.txt]

Repo conventions to follow:
- Expected directory layout: [paste or describe]
- Similar service in repo to copy patterns from: [file path or “none yet”]

Deliverables:
1) Minimal runnable service with:
   - /health and /ready endpoints
   - structured logging
   - metrics endpoint (if applicable)
   - config loading with env vars
2) Minimal CI integration (lint/typecheck/test)
3) Minimal README in the service folder explaining how to run it

Before writing any code:
- propose the exact file structure and why it matches the framework conventions
- list required env vars and defaults
- list what will be mocked vs real in dev

Implementation requirements:
- No placeholders in health/ready logic
- Ready must verify critical dependencies; Health must be minimal
- Add at least:
  - 2 unit tests
  - 1 integration test (if it touches DB/Redis)

Output:
- Provide a step-by-step plan
- Then provide the exact diffs or file contents to create
- Then list verification commands
