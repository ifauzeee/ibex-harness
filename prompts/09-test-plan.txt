Role: Test Writer.

We need a test plan for:
- Feature/service: [name]
- Critical risks: [list]

Constraints / invariants:
[Paste contents of prompts/00-invariants.txt]

Provide:
1) Unit test suite outline (test names + what each verifies)
2) Integration tests (real Postgres/Redis/ClickHouse/MinIO as needed)
3) Cross-tenant isolation tests (if any data access)
4) Failure mode tests (timeouts, dependency down, retries)
5) Performance tests/benchmarks (if hot path)
6) What must not be mocked and why
7) Expected CI runtime impact and how to keep it reasonable

Do not propose tests that only assert mocks were called.
Prefer tests that verify observable behavior.
