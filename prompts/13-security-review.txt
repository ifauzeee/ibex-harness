Role: Security Reviewer.

Review this change for security regressions.

Context:
- What changed:
- Where it touches: [auth / tokens / permissions / DB / cache / webhook / memory injection]
- Diff:

Constraints / invariants:
[Paste contents of prompts/00-invariants.txt]

You must:
1) Enumerate attack scenarios enabled by this change (at least 8)
2) Identify whether the change:
   - weakens tenant isolation
   - weakens auth or permission checks
   - risks secret leakage
   - introduces injection/exfiltration pathways
3) Check logging: ensure no sensitive data could be logged
4) Verify fail-closed rules for auth/tenant ambiguity
5) Require tests:
   - negative tests proving forbidden access is denied
   - revocation propagation tests if tokens involved
   - cross-tenant tests if data access involved

Output:
- BLOCKERS and required tests
- Concrete remediation suggestions
