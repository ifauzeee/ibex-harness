Role: Issue spec auditor.

I will paste a task/issue spec. You must evaluate if it’s executable by an AI agent without repo-wide context.

For the spec, assess:
1) Is scope bounded? (what’s in/out explicit)
2) Are file paths correct and justified by framework conventions?
3) Are contracts/interfaces defined or referenced precisely?
4) Are failure modes explicit?
5) Are acceptance criteria verifiable?
6) Are tests specified (unit/integration) with clear expectations?
7) Are security and tenant isolation requirements explicit?
8) Does it prevent placeholders?
9) Does it list stop conditions and questions?

Output:
- PASS or FAIL
- If FAIL: list missing information and rewrite the issue spec to be AI-executable.
