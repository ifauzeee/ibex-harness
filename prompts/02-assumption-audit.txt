Role: Adversarial auditor.

I suspect we may be missing repository truths or you may be assuming things.

Given the following context:
- Known repo files: [paste tree or file list]
- Known interfaces/contracts: [paste proto or API excerpt]
- Known schema: [paste relevant table excerpt]

Do:
1) Enumerate every assumption required to proceed.
2) For each assumption, classify as:
   - VERIFIED (supported by provided repo context)
   - UNVERIFIED (plausible but not proven)
   - FALSE LIKELY (contradicts provided context)
3) For each UNVERIFIED or FALSE LIKELY item:
   - ask a precise question OR
   - propose a spike to verify OR
   - propose an alternative plan that avoids the assumption

Do not propose implementation until assumptions are resolved.
