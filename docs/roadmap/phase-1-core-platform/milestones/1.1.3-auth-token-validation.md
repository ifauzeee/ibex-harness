# Milestone 1.1.3: Auth Token Validation

**Status:** Planned  
**Goal:** [1.1 Persistence and auth data plane](../goals.md#goal-11-persistence-and-auth-data-plane)

## Summary

Implement auth service token validation: lookup PAT by prefix/hash, Argon2id verify, return permission bitmap and org context via gRPC. Fail closed on all errors.

## Branch

`feature/m1-1-3-auth-validate-token`

## PR title (example)

`feat(auth): validate PAT against Postgres (m1.1.3)`

## Estimated effort

4–5 days

## Prerequisites

- [1.1.1](1.1.1-postgres-migrations.md) merged
- [1.1.2](1.1.2-auth-proto-and-codegen.md) merged
- Local `buf generate` for Go gRPC stubs

## Tasks

- [ ] Add gRPC server to `services/auth` (`internal/grpc/` or `internal/server/`)
- [ ] Config: gRPC listen port, Postgres pool, migration awareness
- [ ] Repository layer: `tokens` lookup by token identifier (never log raw token)
- [ ] Service layer: Argon2id verify using constant-time compare; map to `ValidateTokenResponse`
- [ ] Set `SET LOCAL app.current_org_id` per transaction
- [ ] Wire gRPC + HTTP on shared process or separate listener per FILE_STRUCTURE
- [ ] Unit tests: hash verify, invalid token, revoked token
- [ ] Integration tests: seed two orgs; cross-tenant negative case
- [ ] Update `services/auth/README.md` with run instructions and grpcurl examples
- [ ] Metrics: validate latency histogram, error counter (low cardinality)

## Files affected

| Path | Action |
| --- | --- |
| `services/auth/internal/config/` | Extend |
| `services/auth/internal/repository/` | Add |
| `services/auth/internal/service/` or `internal/auth/` | Add |
| `services/auth/internal/grpc/` | Add |
| `services/auth/cmd/auth/main.go` | Wire gRPC |
| `services/auth/.env.example` | gRPC port, pool size |
| `go.mod` | gRPC, pgx, argon2 dependencies per DEPENDENCIES.md |
| `docs/ENVIRONMENT_VARIABLES.md` | Auth service vars |

## Testing requirements

- Unit: Argon2id, permission mapping, error codes
- Integration: real Postgres + migrations; cross-tenant
- Contract: proto round-trip (optional buf connect test)

## Documentation updates

- services/auth/README.md
- ENVIRONMENT_VARIABLES.md
- SECURITY.md cross-reference if new threat addressed

## Definition of done

- [ ] `ValidateToken` returns success for valid seeded token in dev DB
- [ ] Invalid/revoked/missing token returns unauthenticated without leaking existence
- [ ] No raw tokens in logs
- [ ] `go test ./services/auth/...` passes including integration
- [ ] `/health` and `/ready` still correct; ready checks Postgres

## Risks

| Risk | Mitigation |
| --- | --- |
| Timing attacks on verify | Use library primitives; no custom crypto |
| Connection pool without org context | Always set `app.current_org_id` in transaction |
