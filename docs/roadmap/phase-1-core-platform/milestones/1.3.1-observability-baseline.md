# Milestone 1.3.1: Observability Baseline

**Status:** Planned  
**Goal:** [1.3 Observability baseline](../goals.md#goal-13-observability-baseline)

## Summary

Wire OpenTelemetry tracer provider (noop exporter acceptable) and migrate metrics to `prometheus/client_golang` for auth and proxy HTTP handlers.

## Branch

`chore/m1-3-1-observability-baseline`

## PR title (example)

`chore(obs): observability baseline (m1.3.1)`

## Estimated effort

3–4 days

## Prerequisites

- [1.2.1](1.2.1-proxy-auth-client.md) merged (middleware exists to instrument)

## Tasks

- [ ] Add OTel SDK deps per DEPENDENCIES.md
- [ ] `internal/observability/` in auth and proxy: tracer provider, shutdown hook
- [ ] HTTP middleware: span per request; propagate `trace_id` in logs
- [ ] Replace custom metrics with Prometheus registry + `promhttp` handler
- [ ] Remove or deprecate custom mutex metrics implementation
- [ ] Document env: `OTEL_EXPORTER_OTLP_ENDPOINT` optional
- [ ] Tests: metrics endpoint returns standard format; span created in integration test when exporter=test

## Files affected

| Path | Action |
| --- | --- |
| `services/auth/internal/metrics/` | Refactor |
| `services/proxy/internal/metrics/` | Refactor |
| `services/auth/cmd/auth/main.go` | OTel init |
| `services/proxy/cmd/proxy/main.go` | OTel init |
| `docs/MONITORING.md` | Note skeleton implementation status |
| `docs/ENVIRONMENT_VARIABLES.md` | OTel vars |

## Testing requirements

- Unit: metrics registration no duplicate panic
- Integration: scrape `/metrics`; assert key series exist

## Definition of done

- [ ] `/metrics` uses prometheus client exposition format
- [ ] Tracer provider starts and shuts down cleanly
- [ ] No high-cardinality labels
- [ ] PERFORMANCE.md stage metrics plan noted for Phase 2

## Risks

| Risk | Mitigation |
| --- | --- |
| Dependency weight | Only core OTel packages |
| Breaking metric names | Document rename in FINDINGS if dashboards existed |
