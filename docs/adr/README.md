# Architecture Decision Records (moved)

ADR content has moved to the docs site source at `docs/app/content/docs/adr/`.

- **Public site:** `/docs/adr` (when deployed) or run the docs app locally and open `/docs/adr`.
- **Contributor edits:** change MDX under `docs/app/content/docs/adr/`, then run `pnpm exec fumadocs-mdx` in `docs/app/`.
- **New ADRs:** copy `ADR-0001-template.md` workflow from git history or use the index page template section; assign the next sequential number.

Cross-links from roadmap decision logs point to `/docs/adr/*`.
