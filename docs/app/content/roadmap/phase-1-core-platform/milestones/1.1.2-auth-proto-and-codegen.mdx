---
title: "1.1.2 Auth Proto"
description: "Milestone 1.1.2: Auth Protobuf and Codegen — complete."
summary: "Milestone 1.1.2: Auth Protobuf and Codegen — complete."
status: "completed"
milestoneId: "1.1.2"
goal: "[1.1 Persistence and auth data plane](/roadmap/phase-1-core-platform/goals#goal-11-persistence-and-auth-data-plane)"
phase: "phase-1-core-platform"
fullTitle: "Milestone 1.1.2: Auth Protobuf and Codegen"
---

# Milestone 1.1.2: Auth Protobuf and Codegen

**Status:** Complete  
**Goal:** [1.1 Persistence and auth data plane](/roadmap/phase-1-core-platform/goals#goal-11-persistence-and-auth-data-plane)

## Summary

Add `ibex.auth.v1` protobuf definitions for token validation (and future auth RPCs), register in Buf module, document local `buf generate` for Go stubs without committing `gen/`.

## Branch

`chore/m1-1-2-auth-proto-codegen` (merged)

## PR title (example)

`chore(proto): auth protobuf and codegen (m1.1.2)`

## Estimated effort

2 days

## Prerequisites

- [1.1.1](/roadmap/phase-1-core-platform/milestones/1.1.1-postgres-migrations) merged (schema exists for later implementation)

## Tasks

- [x] ADR-0006: auth proto package layout, RPC naming, error model
- [x] Create `packages/proto/proto/ibex/auth/v1/auth.proto` with `AuthService`:
  - [x] `ValidateToken(ValidateTokenRequest) returns (ValidateTokenResponse)`
  - [x] Messages: token string, org_id, agent_id optional, permission bitmap, expiry
  - [x] `go_package` option aligned with ADR-0004
- [x] Update `packages/proto/buf.yaml` / lint if needed for new package
- [x] Update `packages/proto/buf.gen.yaml` for Go output path under `packages/proto/gen/go/...`
- [x] Document local generation in `packages/proto/README.md`
- [x] CI: `buf lint` includes auth package; breaking check against `main` baseline
- [x] Add `.gitignore` confirmation for `gen/` (already policy)
- [x] Optional: `make proto-gen` target (local only) in dev-tool.sh
- [x] Contract tests: `make proto-test`, `make proto-test-integration`; CI `proto-contract` job

## Files affected

| Path | Action |
| --- | --- |
| `packages/proto/proto/ibex/auth/v1/auth.proto` | Add |
| `packages/proto/README.md` | Update |
| `docs/adr/ADR-0006-auth-proto-contract.md` | Add |
| `docs/API_DOCUMENTATION.md` | gRPC section reference if internal API documented |
| `infra/scripts/dev-tool.sh` | Optional `proto-gen` |
| `packages/proto/auth_contract_test.go` | Add |
| `packages/proto/auth_contract_integration_test.go` | Add |

## Testing requirements

- `buf lint` pass
- `buf breaking` pass against main (after first merge, baseline exists)
- No generated files committed
- `make proto-test` and `make proto-test-integration` pass

## Documentation updates

- ADR-0006
- packages/proto/README.md
- roadmap CURRENT_STATE after merge

## Definition of done

- [x] Auth proto reviewed against ARCHITECTURE.md auth validation pipeline
- [x] Buf CI green
- [x] Local `buf generate` produces importable Go stubs for auth service work
- [x] No service implementation in this PR
- [x] Contract unit and integration tests in CI

## Risks

| Risk | Mitigation |
| --- | --- |
| Proto churn before implementation | Keep v1 minimal; breaking changes via buf breaking |
| Import path mismatch | Match root module `github.com/Rick1330/ibex-harness/...` |

## Merge

Squash merge [PR #13](https://github.com/Rick1330/ibex-harness/pull/13) → `main` at **`343e8c3`**.