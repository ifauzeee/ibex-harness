{
  "name": "ibex-harness",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "docs:dev": "turbo dev --filter=docs",
    "docs:build": "turbo build --filter=docs"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  },
  "pnpm": {
    "overrides": {
      "esbuild": ">=0.28.1",
      "postcss": ">=8.5.10"
    }
  }
}
