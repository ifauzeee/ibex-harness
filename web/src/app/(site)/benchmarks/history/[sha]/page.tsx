import type { Metadata } from "next";  import { BenchmarkPageShell } from "@/components/benchmarks/benchmark-page-shell"; import { BenchmarkRunDetailPanel } from "@/components/benchmarks/lazy-panels"; import { loadPublishedBenchmarkRuns } from "@/lib/benchmarks/published-data";  type RunDetailPageProps = Readonly<{   params: Promise<{ sha: string }>; }>;  export const dynamic = "force-static";  export async function generateStaticParams() {   return loadPublishedBenchmarkRuns().map((run) => ({ sha: run.short_sha })); }  export async function generateMetadata(props: RunDetailPageProps): Promise<Metadata> {   const { sha } = await props.params;   return {     title: `Benchmarks ΓÇö Run ${sha}`,     description: `Benchmark run detail for commit ${sha}.`,   }; }  export default async function BenchmarkRunDetailPage(props: RunDetailPageProps) {   const { sha } = await props.params;   return (     <BenchmarkPageShell       title={`Run ${sha}`}       subtitle="Full benchmark output for this commit."     >       <BenchmarkRunDetailPanel sha={sha} />     </BenchmarkPageShell>   ); }